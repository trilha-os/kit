# Modelo de variáveis de ambiente. Copie pra um arquivo chamado .env e preencha.
# O .env nunca sobe pro GitHub (já está no .gitignore).

# Exemplo (preencha só o que você for usar):
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=
